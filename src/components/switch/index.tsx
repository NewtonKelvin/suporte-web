import Switch, { SwitchProps } from "@mui/material/Switch";

const CustomSwitch = () => {
  return <Switch defaultChecked inputProps={{ "aria-label": "ant design" }} />;
};

export default CustomSwitch;
